# MCP OAuth Connection — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permitir que Claude/ChatGPT se conecten al servidor MCP de Mentio vía OAuth 2.1 (PKCE + Dynamic Client Registration) sin que el usuario pegue ninguna API key.

**Architecture:** Authorization server OAuth 2.1 propio, self-hosted dentro de la misma app Next.js (App Router), desplegado con Vercel. Emite tokens opacos hasheados (patrón de `mcp_api_keys`), revocables. El endpoint `/api/mcp` existente aprende a validar tanto las API keys `mnt_live_` actuales como los nuevos access tokens `mnt_at_`. Reutiliza la sesión Supabase para el login del consentimiento. No usa el SDK de MCP (evita el conflicto zod v3/v4): implementa los estándares directamente.

**Tech Stack:** Next.js 16 (App Router, route handlers, `runtime = "nodejs"`), Supabase (service role para el MCP; cliente con cookies para la sesión), PostgreSQL, `node:crypto` para hashing/PKCE, Biome 2.x linter.

**Spec:** `docs/superpowers/specs/2026-07-15-mcp-oauth-connection-design.md`

## Global Constraints

- **Sin tests unitarios:** el proyecto no tiene runner de tests. Cada tarea se verifica con `pnpm exec tsc --noEmit`, `pnpm lint`, y `curl` funcional contra `http://localhost:3000`. NO instalar vitest/jest.
- **Tokens opacos hasheados sha256.** Prefijos: `mnt_live_` (API keys existentes), `mnt_at_` (access token OAuth), `mnt_rt_` (refresh token OAuth), `mnt_ac_` (authorization code).
- **Owners/admins únicamente** pueden autorizar y gestionar: criterio `can_manage_workspace(workspace_id)` (que es `role in ('owner','admin')`).
- **Clientes OAuth públicos** (sin `client_secret`), PKCE `S256` obligatorio.
- **Route handlers:** `export const runtime = "nodejs"` y `export const dynamic = "force-dynamic"`.
- **Server Actions:** `"use server"`, `createClient()` de `@/lib/supabase/server`, permisos con `supabase.rpc("can_manage_workspace", { p_workspace_id })`, retorno `ActionResult<T>`, nunca `redirect()` dentro de una action.
- **Biome:** `type="button"` explícito en botones, `aria-hidden="true"` en SVG decorativos, `noExplicitAny` es error, `noNonNullAssertion` warn (permitido en env vars).
- **Base URL:** `process.env.MCP_PUBLIC_BASE_URL ?? "https://neogeo-three.vercel.app"`, sin barra final (`.replace(/\/$/, "")`).
- **Migraciones:** archivo nuevo en `supabase/migrations/` + aplicar con la herramienta MCP `mcp__supabase__apply_migration`. No editar `scripts/migrate.ts`.
- **Git:** rama `codex/mcp-oauth`. Commits frecuentes. No hacer `git push` sin confirmación del usuario.

---

## File Structure

**Nuevos:**
- `supabase/migrations/0043_mcp_oauth.sql` — 3 tablas (`mcp_oauth_clients`, `mcp_oauth_codes`, `mcp_oauth_tokens`) + índices + RLS.
- `src/lib/mcp/oauth.ts` — lógica OAuth pura: generación de códigos/tokens por prefijo, verificación PKCE S256, base URL, tipos.
- `src/app/.well-known/oauth-protected-resource/route.ts` — metadata RFC 9728.
- `src/app/.well-known/oauth-authorization-server/route.ts` — metadata RFC 8414.
- `src/app/api/mcp/oauth/register/route.ts` — Dynamic Client Registration (RFC 7591).
- `src/app/api/mcp/oauth/authorize/route.ts` — GET (login+consentimiento en HTML) y POST (emite código).
- `src/app/api/mcp/oauth/token/route.ts` — intercambio de código y refresh.
- `scripts/mcp-oauth-smoke.ts` — smoke test reproducible del flujo completo (curl-equivalente en tsx).

**Modificados:**
- `src/lib/mcp/auth.ts` — resolución dual de token por prefijo; exportar helper de hashing reutilizable.
- `src/app/api/mcp/route.ts` — cabecera `WWW-Authenticate` con `resource_metadata` en el 401.
- `middleware.ts` — excluir del matcher los paths públicos nuevos.
- `src/actions/mcp.ts` — abrir a `can_manage_workspace`; añadir listar/revocar conexiones OAuth.
- `src/components/workspace/McpKeysPanel.tsx` — sección "Conexiones OAuth activas" con revocar.
- `src/app/(app)/[workspace]/mcp/page.tsx` — gate `isSuperAdmin` → `can_manage_workspace`; pasar conexiones OAuth.
- `src/components/layout/MainNav.tsx` — ítem MCP `superAdminOnly` → `ownerOnly`; quitar "(beta)".
- `docs/mcp-server.md` — documentar el flujo OAuth para clientes.

---

## Task 1: Migración de base de datos

**Files:**
- Create: `supabase/migrations/0043_mcp_oauth.sql`

**Interfaces:**
- Produces: tablas `public.mcp_oauth_clients`, `public.mcp_oauth_codes`, `public.mcp_oauth_tokens` con las columnas usadas por las tareas siguientes.

- [ ] **Step 1: Escribir la migración SQL**

Create `supabase/migrations/0043_mcp_oauth.sql`:

```sql
-- OAuth 2.1 para el servidor MCP (/api/mcp).
-- Permite que clientes MCP (Claude, ChatGPT) se conecten sin pegar API key:
-- se registran solos (DCR), el usuario autoriza en una pantalla de consentimiento,
-- y reciben tokens opacos (solo se guarda el hash sha256). Solo lectura, un token
-- resuelve a un único workspace. Convive con mcp_api_keys (keys manuales mnt_live_).

-- Clientes MCP que se auto-registran (Dynamic Client Registration, RFC 7591).
create table if not exists public.mcp_oauth_clients (
  client_id     text primary key,
  client_name   text,
  redirect_uris text[] not null,
  created_at    timestamptz not null default now()
);

comment on table public.mcp_oauth_clients is
  'Clientes MCP registrados vía Dynamic Client Registration. Públicos (sin secreto), PKCE obligatorio.';

-- Códigos de autorización efímeros (un solo uso, ~60s).
create table if not exists public.mcp_oauth_codes (
  id                    uuid primary key default gen_random_uuid(),
  code_hash             text not null unique,
  client_id             text not null references public.mcp_oauth_clients(client_id) on delete cascade,
  workspace_id          uuid not null references public.workspaces(id) on delete cascade,
  user_id               uuid not null references auth.users(id) on delete cascade,
  redirect_uri          text not null,
  code_challenge        text not null,
  code_challenge_method text not null default 'S256',
  scope                 text,
  expires_at            timestamptz not null,
  consumed_at           timestamptz,
  created_at            timestamptz not null default now()
);

create index if not exists mcp_oauth_codes_hash_idx on public.mcp_oauth_codes (code_hash) where consumed_at is null;

comment on table public.mcp_oauth_codes is
  'Códigos de autorización OAuth de un solo uso. Ligados a PKCE (code_challenge S256).';

-- Tokens emitidos (access + refresh). Mismo patrón hash que mcp_api_keys.
create table if not exists public.mcp_oauth_tokens (
  id           uuid primary key default gen_random_uuid(),
  token_hash   text not null unique,
  refresh_hash text unique,
  client_id    text not null references public.mcp_oauth_clients(client_id) on delete cascade,
  workspace_id uuid not null references public.workspaces(id) on delete cascade,
  user_id      uuid not null references auth.users(id) on delete cascade,
  client_name  text,
  expires_at   timestamptz not null,
  revoked_at   timestamptz,
  last_used_at timestamptz,
  created_at   timestamptz not null default now()
);

create index if not exists mcp_oauth_tokens_access_idx on public.mcp_oauth_tokens (token_hash) where revoked_at is null;
create index if not exists mcp_oauth_tokens_refresh_idx on public.mcp_oauth_tokens (refresh_hash) where revoked_at is null;
create index if not exists mcp_oauth_tokens_workspace_idx on public.mcp_oauth_tokens (workspace_id);

comment on table public.mcp_oauth_tokens is
  'Tokens OAuth de solo lectura para el servidor MCP. Solo se guarda el hash. Revocables (revoked_at).';

-- RLS. La resolución/emisión de tokens la hace solo el service role (bypassea RLS).
-- Los gestores del workspace pueden LISTAR sus conexiones OAuth para revocarlas.
alter table public.mcp_oauth_clients enable row level security;
alter table public.mcp_oauth_codes   enable row level security;
alter table public.mcp_oauth_tokens  enable row level security;

drop policy if exists "mcp_oauth_tokens_select_managers" on public.mcp_oauth_tokens;
create policy "mcp_oauth_tokens_select_managers" on public.mcp_oauth_tokens
  for select using (public.can_manage_workspace(workspace_id));
```

- [ ] **Step 2: Aplicar la migración**

Usar la herramienta MCP de Supabase `mcp__supabase__apply_migration` con `name: "0043_mcp_oauth"` y el SQL de arriba.

- [ ] **Step 3: Verificar que las tablas existen**

Ejecutar vía `mcp__supabase__execute_sql`:
```sql
select table_name from information_schema.tables
where table_schema='public' and table_name like 'mcp_oauth_%' order by table_name;
```
Expected: 3 filas — `mcp_oauth_clients`, `mcp_oauth_codes`, `mcp_oauth_tokens`.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/0043_mcp_oauth.sql
git commit -m "feat(mcp): migración 0043 tablas OAuth (clients/codes/tokens)"
```

---

## Task 2: Helpers OAuth (`src/lib/mcp/oauth.ts`)

**Files:**
- Create: `src/lib/mcp/oauth.ts`

**Interfaces:**
- Consumes: `hashApiKey` de `src/lib/mcp/auth.ts`.
- Produces:
  - `oauthBaseUrl(): string`
  - `generateOpaqueToken(prefix: string): { token: string; hash: string }`
  - `verifyPkceS256(codeVerifier: string, codeChallenge: string): boolean`
  - Constantes: `ACCESS_PREFIX = "mnt_at_"`, `REFRESH_PREFIX = "mnt_rt_"`, `CODE_PREFIX = "mnt_ac_"`, `ACCESS_TTL_SECONDS = 3600`, `CODE_TTL_SECONDS = 60`.

- [ ] **Step 1: Escribir el módulo**

Create `src/lib/mcp/oauth.ts`:

```ts
import { createHash, randomBytes } from "node:crypto";
import { hashApiKey } from "@/lib/mcp/auth";

export const ACCESS_PREFIX = "mnt_at_";
export const REFRESH_PREFIX = "mnt_rt_";
export const CODE_PREFIX = "mnt_ac_";

export const ACCESS_TTL_SECONDS = 3600; // 1h
export const CODE_TTL_SECONDS = 60; // 1min

/** Base pública del servidor OAuth/MCP, sin barra final. */
export function oauthBaseUrl(): string {
  const base = process.env.MCP_PUBLIC_BASE_URL ?? "https://neogeo-three.vercel.app";
  return base.replace(/\/$/, "");
}

/** Genera un token/código opaco con prefijo y su hash sha256. El valor en claro no se persiste. */
export function generateOpaqueToken(prefix: string): { token: string; hash: string } {
  const secret = randomBytes(24).toString("hex"); // 48 chars
  const token = `${prefix}${secret}`;
  return { token, hash: hashApiKey(token) };
}

/** Verifica PKCE S256: base64url(sha256(code_verifier)) === code_challenge. */
export function verifyPkceS256(codeVerifier: string, codeChallenge: string): boolean {
  if (!codeVerifier || !codeChallenge) return false;
  const computed = createHash("sha256").update(codeVerifier).digest("base64url");
  return computed === codeChallenge;
}
```

- [ ] **Step 2: Verificar tipos y lint**

Run: `pnpm exec tsc --noEmit && pnpm lint`
Expected: sin errores en `src/lib/mcp/oauth.ts`.

- [ ] **Step 3: Verificar el PKCE con un caso conocido**

Run:
```bash
node -e "const {createHash}=require('crypto'); const v='dBjftJeZ4CVP-mB92K27uhbUJU1p1r_wW1gFWFOEjXk'; const c=createHash('sha256').update(v).digest('base64url'); console.log(c==='E9Melhoa2OwvFrEMTJguCHaoeK1t8URWbuGJSstw-cM');"
```
Expected: `true` (vector de ejemplo de la RFC 7636).

- [ ] **Step 4: Commit**

```bash
git add src/lib/mcp/oauth.ts
git commit -m "feat(mcp): helpers OAuth (tokens opacos, PKCE S256, base url)"
```

---

## Task 3: Resolución dual de token en `auth.ts`

**Files:**
- Modify: `src/lib/mcp/auth.ts`

**Interfaces:**
- Consumes: tabla `mcp_oauth_tokens` (Task 1), constante `ACCESS_PREFIX` (Task 2).
- Produces: `resolveWorkspaceFromAuth` acepta también access tokens `mnt_at_`, devolviendo el mismo `ResolvedWorkspace`.

- [ ] **Step 1: Refactor — extraer la carga de workspace y añadir la rama OAuth**

En `src/lib/mcp/auth.ts`, reemplazar el cuerpo de `resolveWorkspaceFromAuth` (desde `const supabase = mcpServiceClient();` hasta el `return { ... }` final) por:

```ts
  const supabase = mcpServiceClient();

  // Access token OAuth (mnt_at_): tabla mcp_oauth_tokens.
  if (token.startsWith("mnt_at_")) {
    const tokenHash = hashApiKey(token);
    const { data: tok } = await supabase
      .from("mcp_oauth_tokens")
      .select("id, workspace_id, revoked_at, expires_at")
      .eq("token_hash", tokenHash)
      .maybeSingle();

    if (!tok || tok.revoked_at || new Date(tok.expires_at as string) < new Date()) return null;

    const workspace = await loadWorkspace(supabase, tok.workspace_id as string);
    if (!workspace) return null;

    void supabase
      .from("mcp_oauth_tokens")
      .update({ last_used_at: new Date().toISOString() })
      .eq("id", tok.id)
      .then(() => undefined);

    return workspace;
  }

  // API key manual (mnt_live_): tabla mcp_api_keys (comportamiento existente).
  const keyHash = hashApiKey(token);
  const { data: keyRow, error } = await supabase
    .from("mcp_api_keys")
    .select("id, workspace_id, revoked_at")
    .eq("key_hash", keyHash)
    .maybeSingle();

  if (error || !keyRow || keyRow.revoked_at) return null;

  const workspace = await loadWorkspace(supabase, keyRow.workspace_id as string);
  if (!workspace) return null;

  void supabase
    .from("mcp_api_keys")
    .update({ last_used_at: new Date().toISOString() })
    .eq("id", keyRow.id)
    .then(() => undefined);

  return workspace;
```

- [ ] **Step 2: Añadir el helper `loadWorkspace` al final del archivo**

Añadir en `src/lib/mcp/auth.ts`:

```ts
/** Carga y mapea un workspace a ResolvedWorkspace. Compartido por las dos vías de auth. */
async function loadWorkspace(
  supabase: SupabaseClient,
  workspaceId: string
): Promise<ResolvedWorkspace | null> {
  const { data: workspace } = await supabase
    .from("workspaces")
    .select("id, slug, name, brand_name, domain, country")
    .eq("id", workspaceId)
    .single();

  if (!workspace) return null;

  return {
    workspaceId: workspace.id as string,
    slug: workspace.slug as string,
    name: workspace.name as string,
    brandName: (workspace.brand_name as string | null) ?? null,
    domain: (workspace.domain as string | null) ?? null,
    country: (workspace.country as string | null) ?? null,
  };
}
```

- [ ] **Step 3: Verificar tipos y lint**

Run: `pnpm exec tsc --noEmit && pnpm lint`
Expected: sin errores. `SupabaseClient` ya está importado en el archivo.

- [ ] **Step 4: Verificar que la API key existente sigue funcionando (no-regresión)**

Con el dev server corriendo y la key activa de Air Europa (`mnt_live_7fb12f28...`):
```bash
curl -s -X POST http://localhost:3000/api/mcp \
  -H "Authorization: Bearer mnt_live_7fb12f2870fab6b70875509dd540f4efc659a97d9b86be79" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"get_workspace_overview","arguments":{}}}'
```
Expected: JSON con `"name": "Air Europa"`.

- [ ] **Step 5: Commit**

```bash
git add src/lib/mcp/auth.ts
git commit -m "feat(mcp): resolveWorkspaceFromAuth valida access tokens OAuth (mnt_at_)"
```

---

## Task 4: Endpoints de metadata (.well-known)

**Files:**
- Create: `src/app/.well-known/oauth-protected-resource/route.ts`
- Create: `src/app/.well-known/oauth-authorization-server/route.ts`

**Interfaces:**
- Consumes: `oauthBaseUrl()` (Task 2).
- Produces: dos endpoints GET públicos que devuelven JSON de descubrimiento OAuth.

- [ ] **Step 1: Protected Resource Metadata (RFC 9728)**

Create `src/app/.well-known/oauth-protected-resource/route.ts`:

```ts
import { NextResponse } from "next/server";
import { oauthBaseUrl } from "@/lib/mcp/oauth";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const CORS = { "Access-Control-Allow-Origin": "*" };

export function GET() {
  const base = oauthBaseUrl();
  return NextResponse.json(
    {
      resource: `${base}/api/mcp`,
      authorization_servers: [base],
      bearer_methods_supported: ["header"],
    },
    { headers: CORS }
  );
}

export function OPTIONS() {
  return new NextResponse(null, { status: 204, headers: CORS });
}
```

- [ ] **Step 2: Authorization Server Metadata (RFC 8414)**

Create `src/app/.well-known/oauth-authorization-server/route.ts`:

```ts
import { NextResponse } from "next/server";
import { oauthBaseUrl } from "@/lib/mcp/oauth";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const CORS = { "Access-Control-Allow-Origin": "*" };

export function GET() {
  const base = oauthBaseUrl();
  return NextResponse.json(
    {
      issuer: base,
      authorization_endpoint: `${base}/api/mcp/oauth/authorize`,
      token_endpoint: `${base}/api/mcp/oauth/token`,
      registration_endpoint: `${base}/api/mcp/oauth/register`,
      response_types_supported: ["code"],
      grant_types_supported: ["authorization_code", "refresh_token"],
      code_challenge_methods_supported: ["S256"],
      token_endpoint_auth_methods_supported: ["none"],
      scopes_supported: ["mcp:read"],
    },
    { headers: CORS }
  );
}

export function OPTIONS() {
  return new NextResponse(null, { status: 204, headers: CORS });
}
```

- [ ] **Step 3: Verificar tipos y lint**

Run: `pnpm exec tsc --noEmit && pnpm lint`
Expected: sin errores.

- [ ] **Step 4: Verificar los endpoints (requiere Task 9 para el middleware; por ahora probar con dev server)**

```bash
curl -s http://localhost:3000/.well-known/oauth-authorization-server | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);console.log(j.authorization_endpoint, j.code_challenge_methods_supported)})"
```
Expected: `.../api/mcp/oauth/authorize [ 'S256' ]`. (Si redirige a /login, se arregla en Task 9.)

- [ ] **Step 5: Commit**

```bash
git add src/app/.well-known
git commit -m "feat(mcp): endpoints de metadata OAuth (.well-known)"
```

---

## Task 5: Dynamic Client Registration (`/register`)

**Files:**
- Create: `src/app/api/mcp/oauth/register/route.ts`

**Interfaces:**
- Consumes: `mcpServiceClient` (auth.ts), tabla `mcp_oauth_clients` (Task 1).
- Produces: `POST /api/mcp/oauth/register` → `{ client_id, client_name, redirect_uris, token_endpoint_auth_method, grant_types, response_types }` con status 201.

- [ ] **Step 1: Escribir el endpoint**

Create `src/app/api/mcp/oauth/register/route.ts`:

```ts
import { randomUUID } from "node:crypto";
import { type NextRequest, NextResponse } from "next/server";
import { mcpServiceClient } from "@/lib/mcp/auth";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const CORS = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Methods": "POST, OPTIONS",
  "Access-Control-Allow-Headers": "Content-Type",
};

export async function POST(request: NextRequest) {
  let body: { client_name?: string; redirect_uris?: unknown };
  try {
    body = await request.json();
  } catch {
    return NextResponse.json({ error: "invalid_request" }, { status: 400, headers: CORS });
  }

  const redirectUris = Array.isArray(body.redirect_uris)
    ? body.redirect_uris.filter((u): u is string => typeof u === "string")
    : [];

  if (redirectUris.length === 0) {
    return NextResponse.json(
      { error: "invalid_redirect_uri", error_description: "redirect_uris requerido" },
      { status: 400, headers: CORS }
    );
  }

  const clientId = `mcp_${randomUUID().replace(/-/g, "")}`;
  const clientName = typeof body.client_name === "string" ? body.client_name : null;

  const service = mcpServiceClient();
  const { error } = await service.from("mcp_oauth_clients").insert({
    client_id: clientId,
    client_name: clientName,
    redirect_uris: redirectUris,
  });
  if (error) {
    return NextResponse.json({ error: "server_error" }, { status: 500, headers: CORS });
  }

  return NextResponse.json(
    {
      client_id: clientId,
      client_name: clientName,
      redirect_uris: redirectUris,
      token_endpoint_auth_method: "none",
      grant_types: ["authorization_code", "refresh_token"],
      response_types: ["code"],
    },
    { status: 201, headers: CORS }
  );
}

export function OPTIONS() {
  return new NextResponse(null, { status: 204, headers: CORS });
}
```

- [ ] **Step 2: Verificar tipos y lint**

Run: `pnpm exec tsc --noEmit && pnpm lint`
Expected: sin errores.

- [ ] **Step 3: Verificar registro (tras Task 9 para el middleware)**

```bash
curl -s -X POST http://localhost:3000/api/mcp/oauth/register \
  -H "Content-Type: application/json" \
  -d '{"client_name":"Smoke","redirect_uris":["https://example.com/callback"]}'
```
Expected: JSON con `client_id` empezando por `mcp_` y status 201.

- [ ] **Step 4: Commit**

```bash
git add src/app/api/mcp/oauth/register
git commit -m "feat(mcp): Dynamic Client Registration OAuth (/register)"
```

---

## Task 6: Authorize — login + consentimiento (`/authorize`)

**Files:**
- Create: `src/app/api/mcp/oauth/authorize/route.ts`

**Interfaces:**
- Consumes: `createClient` de `@/lib/supabase/server`, `mcpServiceClient` (auth.ts), `generateOpaqueToken`, `CODE_PREFIX`, `CODE_TTL_SECONDS`, `oauthBaseUrl` (Task 2), tabla `mcp_oauth_clients`/`mcp_oauth_codes` (Task 1).
- Produces:
  - `GET /api/mcp/oauth/authorize` → HTML de consentimiento (o redirect a `/login`).
  - `POST /api/mcp/oauth/authorize` → emite código y redirige al `redirect_uri`.

- [ ] **Step 1: Escribir el endpoint (GET + POST) con render HTML**

Create `src/app/api/mcp/oauth/authorize/route.ts`:

```ts
import { type NextRequest, NextResponse } from "next/server";
import { mcpServiceClient } from "@/lib/mcp/auth";
import { CODE_PREFIX, CODE_TTL_SECONDS, generateOpaqueToken, oauthBaseUrl } from "@/lib/mcp/oauth";
import { createClient } from "@/lib/supabase/server";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

interface ManageableWorkspace {
  id: string;
  name: string;
  slug: string;
}

function htmlPage(inner: string): NextResponse {
  return new NextResponse(
    `<!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Conectar con Mentio</title><style>
      body{font-family:system-ui,sans-serif;background:#f8fafc;margin:0;display:flex;min-height:100vh;align-items:center;justify-content:center}
      .card{background:#fff;border:1px solid #e2e8f0;border-radius:12px;padding:28px;max-width:420px;width:90%;box-shadow:0 1px 3px rgba(0,0,0,.06)}
      h1{font-size:18px;margin:0 0 8px;color:#0f172a}p{color:#475569;font-size:14px;line-height:1.5}
      select{width:100%;padding:8px;border:1px solid #cbd5e1;border-radius:8px;margin:12px 0;font-size:14px}
      .row{display:flex;gap:8px;margin-top:16px}button{flex:1;padding:10px;border-radius:8px;font-size:14px;cursor:pointer;border:1px solid #cbd5e1;background:#fff;color:#334155}
      button.primary{background:#4f46e5;color:#fff;border-color:#4f46e5}code{background:#f1f5f9;padding:1px 5px;border-radius:4px;font-size:12px}
    </style></head><body><div class="card">${inner}</div></body></html>`,
    { headers: { "Content-Type": "text/html; charset=utf-8" } }
  );
}

function redirectError(redirectUri: string, state: string | null, err: string): NextResponse {
  const u = new URL(redirectUri);
  u.searchParams.set("error", err);
  if (state) u.searchParams.set("state", state);
  return NextResponse.redirect(u.toString());
}

async function validateClient(clientId: string | null, redirectUri: string | null) {
  if (!clientId || !redirectUri) return null;
  const service = mcpServiceClient();
  const { data } = await service
    .from("mcp_oauth_clients")
    .select("client_id, client_name, redirect_uris")
    .eq("client_id", clientId)
    .maybeSingle();
  if (!data) return null;
  if (!(data.redirect_uris as string[]).includes(redirectUri)) return null;
  return { clientName: (data.client_name as string | null) ?? "una aplicación" };
}

export async function GET(request: NextRequest) {
  const p = request.nextUrl.searchParams;
  const clientId = p.get("client_id");
  const redirectUri = p.get("redirect_uri");
  const state = p.get("state");
  const codeChallenge = p.get("code_challenge");
  const codeChallengeMethod = p.get("code_challenge_method");
  const responseType = p.get("response_type");

  const client = await validateClient(clientId, redirectUri);
  if (!client) return htmlPage(`<h1>Solicitud inválida</h1><p>El cliente o la URL de retorno no son válidos.</p>`);
  if (responseType !== "code") return redirectError(redirectUri!, state, "unsupported_response_type");
  if (codeChallengeMethod !== "S256" || !codeChallenge) return redirectError(redirectUri!, state, "invalid_request");

  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) {
    // La página /login solo acepta redirects RELATIVOS (startsWith("/") y no "//").
    // Por eso `back` debe ser una ruta relativa, no una URL absoluta.
    const back = `/api/mcp/oauth/authorize?${p.toString()}`;
    const loginUrl = new URL("/login", oauthBaseUrl());
    loginUrl.searchParams.set("redirect", back);
    return NextResponse.redirect(loginUrl.toString());
  }

  const { data: memberships } = await supabase
    .from("workspace_members")
    .select("role, workspaces(id, name, slug)")
    .eq("user_id", user.id)
    .in("role", ["owner", "admin"]);

  const workspaces: ManageableWorkspace[] = (memberships ?? [])
    .map((m) => m.workspaces as unknown as ManageableWorkspace)
    .filter(Boolean);

  if (workspaces.length === 0) {
    return htmlPage(`<h1>Sin permisos</h1><p>Tu cuenta no es propietaria ni administradora de ningún workspace, así que no puede conectar por MCP.</p>`);
  }

  const options = workspaces
    .map((w) => `<option value="${w.id}">${w.name}</option>`)
    .join("");

  return htmlPage(`
    <h1>Conectar con Mentio</h1>
    <p><strong>${client.clientName}</strong> quiere leer los datos GEO (solo lectura) de tu workspace:</p>
    <form method="POST">
      ${Array.from(p.entries()).map(([k, v]) => `<input type="hidden" name="${k}" value="${v.replace(/"/g, "&quot;")}">`).join("")}
      <select name="workspace_id">${options}</select>
      <div class="row">
        <button type="submit" name="decision" value="deny">Cancelar</button>
        <button type="submit" name="decision" value="allow" class="primary">Autorizar</button>
      </div>
    </form>`);
}

export async function POST(request: NextRequest) {
  const form = await request.formData();
  const clientId = form.get("client_id") as string | null;
  const redirectUri = form.get("redirect_uri") as string | null;
  const state = form.get("state") as string | null;
  const codeChallenge = form.get("code_challenge") as string | null;
  const workspaceId = form.get("workspace_id") as string | null;
  const decision = form.get("decision") as string | null;

  const client = await validateClient(clientId, redirectUri);
  if (!client) return htmlPage(`<h1>Solicitud inválida</h1><p>Cliente o URL de retorno no válidos.</p>`);
  if (decision !== "allow") return redirectError(redirectUri!, state, "access_denied");
  if (!codeChallenge || !workspaceId) return redirectError(redirectUri!, state, "invalid_request");

  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return redirectError(redirectUri!, state, "access_denied");

  // Re-verificar que el usuario gestiona ESE workspace (no confiar en el form).
  const { data: canManage } = await supabase.rpc("can_manage_workspace", { p_workspace_id: workspaceId });
  if (!canManage) return redirectError(redirectUri!, state, "access_denied");

  const { token: code, hash: codeHash } = generateOpaqueToken(CODE_PREFIX);
  const service = mcpServiceClient();
  const { error } = await service.from("mcp_oauth_codes").insert({
    code_hash: codeHash,
    client_id: clientId,
    workspace_id: workspaceId,
    user_id: user.id,
    redirect_uri: redirectUri,
    code_challenge: codeChallenge,
    code_challenge_method: "S256",
    scope: "mcp:read",
    expires_at: new Date(Date.now() + CODE_TTL_SECONDS * 1000).toISOString(),
  });
  if (error) return redirectError(redirectUri!, state, "server_error");

  const u = new URL(redirectUri!);
  u.searchParams.set("code", code);
  if (state) u.searchParams.set("state", state);
  return NextResponse.redirect(u.toString(), { status: 303 });
}
```

- [ ] **Step 2: Verificar tipos y lint**

Run: `pnpm exec tsc --noEmit && pnpm lint`
Expected: sin errores. Nota: los `!` en `redirectUri!` tras `validateClient` truthy son aceptables (`noNonNullAssertion` es warn).

- [ ] **Step 3: Verificar el redirect a login sin sesión (tras Task 9)**

```bash
curl -s -o /dev/null -w "%{http_code} %{redirect_url}\n" \
  "http://localhost:3000/api/mcp/oauth/authorize?response_type=code&client_id=NOPE&redirect_uri=https://example.com/cb&code_challenge=x&code_challenge_method=S256"
```
Expected: `200` con HTML "Solicitud inválida" (client_id no existe).

- [ ] **Step 4: Commit**

```bash
git add src/app/api/mcp/oauth/authorize
git commit -m "feat(mcp): endpoint authorize OAuth (login + consentimiento owners)"
```

---

## Task 7: Token endpoint (`/token`)

**Files:**
- Create: `src/app/api/mcp/oauth/token/route.ts`

**Interfaces:**
- Consumes: `mcpServiceClient`, `hashApiKey` (auth.ts), `generateOpaqueToken`, `verifyPkceS256`, `ACCESS_PREFIX`, `REFRESH_PREFIX`, `ACCESS_TTL_SECONDS` (Task 2), tablas `mcp_oauth_codes`/`mcp_oauth_tokens` (Task 1).
- Produces: `POST /api/mcp/oauth/token` → `{ access_token, token_type, expires_in, refresh_token, scope }`.

- [ ] **Step 1: Escribir el endpoint**

Create `src/app/api/mcp/oauth/token/route.ts`:

```ts
import { type NextRequest, NextResponse } from "next/server";
import { hashApiKey, mcpServiceClient } from "@/lib/mcp/auth";
import {
  ACCESS_PREFIX,
  ACCESS_TTL_SECONDS,
  generateOpaqueToken,
  REFRESH_PREFIX,
  verifyPkceS256,
} from "@/lib/mcp/oauth";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const CORS = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Methods": "POST, OPTIONS",
  "Access-Control-Allow-Headers": "Content-Type, Authorization",
};

function oauthError(err: string, status = 400) {
  return NextResponse.json({ error: err }, { status, headers: CORS });
}

async function issueTokens(params: {
  clientId: string;
  workspaceId: string;
  userId: string;
  clientName: string | null;
}) {
  const access = generateOpaqueToken(ACCESS_PREFIX);
  const refresh = generateOpaqueToken(REFRESH_PREFIX);
  const service = mcpServiceClient();
  await service.from("mcp_oauth_tokens").insert({
    token_hash: access.hash,
    refresh_hash: refresh.hash,
    client_id: params.clientId,
    workspace_id: params.workspaceId,
    user_id: params.userId,
    client_name: params.clientName,
    expires_at: new Date(Date.now() + ACCESS_TTL_SECONDS * 1000).toISOString(),
  });
  return NextResponse.json(
    {
      access_token: access.token,
      token_type: "Bearer",
      expires_in: ACCESS_TTL_SECONDS,
      refresh_token: refresh.token,
      scope: "mcp:read",
    },
    { headers: CORS }
  );
}

export async function POST(request: NextRequest) {
  const form = await request.formData();
  const grantType = form.get("grant_type") as string | null;
  const service = mcpServiceClient();

  if (grantType === "authorization_code") {
    const code = form.get("code") as string | null;
    const codeVerifier = form.get("code_verifier") as string | null;
    const clientId = form.get("client_id") as string | null;
    const redirectUri = form.get("redirect_uri") as string | null;
    if (!code || !codeVerifier || !clientId || !redirectUri) return oauthError("invalid_request");

    const { data: row } = await service
      .from("mcp_oauth_codes")
      .select("id, client_id, workspace_id, user_id, redirect_uri, code_challenge, expires_at, consumed_at")
      .eq("code_hash", hashApiKey(code))
      .maybeSingle();

    if (!row || row.consumed_at) return oauthError("invalid_grant");
    if (row.client_id !== clientId || row.redirect_uri !== redirectUri) return oauthError("invalid_grant");
    if (new Date(row.expires_at as string) < new Date()) return oauthError("invalid_grant");
    if (!verifyPkceS256(codeVerifier, row.code_challenge as string)) return oauthError("invalid_grant");

    await service.from("mcp_oauth_codes").update({ consumed_at: new Date().toISOString() }).eq("id", row.id);

    const { data: client } = await service
      .from("mcp_oauth_clients")
      .select("client_name")
      .eq("client_id", clientId)
      .maybeSingle();

    return issueTokens({
      clientId,
      workspaceId: row.workspace_id as string,
      userId: row.user_id as string,
      clientName: (client?.client_name as string | null) ?? null,
    });
  }

  if (grantType === "refresh_token") {
    const refreshToken = form.get("refresh_token") as string | null;
    if (!refreshToken) return oauthError("invalid_request");

    const { data: row } = await service
      .from("mcp_oauth_tokens")
      .select("id, client_id, workspace_id, user_id, client_name, revoked_at")
      .eq("refresh_hash", hashApiKey(refreshToken))
      .maybeSingle();

    if (!row || row.revoked_at) return oauthError("invalid_grant");

    // Rotación: revoca el token viejo y emite uno nuevo.
    await service.from("mcp_oauth_tokens").update({ revoked_at: new Date().toISOString() }).eq("id", row.id);

    return issueTokens({
      clientId: row.client_id as string,
      workspaceId: row.workspace_id as string,
      userId: row.user_id as string,
      clientName: (row.client_name as string | null) ?? null,
    });
  }

  return oauthError("unsupported_grant_type");
}

export function OPTIONS() {
  return new NextResponse(null, { status: 204, headers: CORS });
}
```

- [ ] **Step 2: Verificar tipos y lint**

Run: `pnpm exec tsc --noEmit && pnpm lint`
Expected: sin errores.

- [ ] **Step 3: Commit**

```bash
git add src/app/api/mcp/oauth/token
git commit -m "feat(mcp): token endpoint OAuth (authorization_code + refresh rotativo)"
```

---

## Task 8: `WWW-Authenticate` en el 401 de `/api/mcp`

**Files:**
- Modify: `src/app/api/mcp/route.ts:96-103`

**Interfaces:**
- Consumes: `oauthBaseUrl` (Task 2).
- Produces: el 401 arranca el descubrimiento OAuth en clientes sin token.

- [ ] **Step 1: Importar el helper**

En `src/app/api/mcp/route.ts`, añadir a los imports:
```ts
import { oauthBaseUrl } from "@/lib/mcp/oauth";
```

- [ ] **Step 2: Reemplazar la cabecera del 401**

Reemplazar el bloque `if (!workspace) { ... }` en `POST` por:

```ts
  const workspace = await resolveWorkspaceFromAuth(request.headers.get("authorization"));
  if (!workspace) {
    const resourceMeta = `${oauthBaseUrl()}/.well-known/oauth-protected-resource`;
    return NextResponse.json(
      error(null, -32001, "No autorizado: se requiere un token válido (Authorization: Bearer <token>)."),
      {
        status: 401,
        headers: {
          ...CORS_HEADERS,
          "WWW-Authenticate": `Bearer resource_metadata="${resourceMeta}"`,
        },
      }
    );
  }
```

- [ ] **Step 3: Verificar tipos, lint y la cabecera**

Run: `pnpm exec tsc --noEmit && pnpm lint`
Luego:
```bash
curl -s -D - -o /dev/null -X POST http://localhost:3000/api/mcp \
  -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","id":1,"method":"ping"}' | grep -i "www-authenticate"
```
Expected: `WWW-Authenticate: Bearer resource_metadata=".../.well-known/oauth-protected-resource"`.

- [ ] **Step 4: Commit**

```bash
git add src/app/api/mcp/route.ts
git commit -m "feat(mcp): 401 con WWW-Authenticate para arrancar el flujo OAuth"
```

---

## Task 9: Exclusiones del middleware

**Files:**
- Modify: `middleware.ts:34-35` y `middleware.ts:46-50`

**Interfaces:**
- Produces: los paths `.well-known` y `/api/mcp/oauth/*` quedan accesibles sin sesión.

- [ ] **Step 1: Añadir los paths públicos al array `publicPaths`**

En `middleware.ts`, reemplazar la línea de `publicPaths` por:
```ts
  const publicPaths = ["/login", "/register", "/auth/callback", "/.well-known"];
```

- [ ] **Step 2: Excluir del matcher los endpoints OAuth JSON**

En el `matcher`, añadir `api/mcp` ya cubre `/api/mcp/oauth/*` (es prefijo). Verificar que el patrón actual excluye `api/mcp` (línea 48). Añadir `.well-known` al negative lookahead:

Reemplazar el matcher por:
```ts
export const config = {
  matcher: [
    "/((?!_next/static|_next/image|favicon.ico|api/inngest|api/internal|api/mcp|\\.well-known|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)",
  ],
};
```

- [ ] **Step 3: Verificar tipos, lint y los endpoints públicos**

Run: `pnpm exec tsc --noEmit && pnpm lint`
Luego:
```bash
curl -s -o /dev/null -w "well-known: %{http_code}\n" http://localhost:3000/.well-known/oauth-authorization-server
curl -s -o /dev/null -w "register: %{http_code}\n" -X POST http://localhost:3000/api/mcp/oauth/register -H "Content-Type: application/json" -d '{"redirect_uris":["https://example.com/cb"]}'
```
Expected: `well-known: 200` y `register: 201` (sin redirección a /login).

- [ ] **Step 4: Commit**

```bash
git add middleware.ts
git commit -m "feat(mcp): excluir .well-known y OAuth del middleware de auth"
```

---

## Task 10: Smoke test end-to-end del flujo OAuth

**Files:**
- Create: `scripts/mcp-oauth-smoke.ts`

**Interfaces:**
- Consumes: todos los endpoints anteriores.
- Produces: script `tsx scripts/mcp-oauth-smoke.ts` que ejercita register → (authorize manual) → token → llamada MCP.

Nota: el paso `authorize` requiere sesión de navegador (login + consentimiento), no automatizable por curl puro. El smoke test cubre las partes automatizables (register, metadata, PKCE, token con un código inyectado en BD) y documenta el paso manual.

- [ ] **Step 1: Escribir el smoke test**

Create `scripts/mcp-oauth-smoke.ts`:

```ts
import { createHash, randomBytes } from "node:crypto";

const BASE = process.env.SMOKE_BASE ?? "http://localhost:3000";

function b64url(buf: Buffer): string {
  return buf.toString("base64url");
}

async function main() {
  // 1. Metadata
  const meta = await fetch(`${BASE}/.well-known/oauth-authorization-server`).then((r) => r.json());
  console.log("✓ metadata:", meta.authorization_endpoint);
  if (!meta.code_challenge_methods_supported?.includes("S256")) throw new Error("falta S256");

  // 2. Dynamic Client Registration
  const reg = await fetch(`${BASE}/api/mcp/oauth/register`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ client_name: "Smoke", redirect_uris: ["https://example.com/cb"] }),
  }).then((r) => r.json());
  console.log("✓ registro client_id:", reg.client_id);
  if (!reg.client_id) throw new Error("registro falló");

  // 3. PKCE
  const verifier = b64url(randomBytes(32));
  const challenge = b64url(createHash("sha256").update(verifier).digest());
  const authUrl = `${meta.authorization_endpoint}?response_type=code&client_id=${reg.client_id}&redirect_uri=${encodeURIComponent("https://example.com/cb")}&code_challenge=${challenge}&code_challenge_method=S256&state=xyz`;
  console.log("\n➡  Abre esta URL en el navegador (logueado como owner), autoriza, y copia el ?code= del redirect:");
  console.log("  ", authUrl);
  console.log("\n   Luego intercámbialo:");
  console.log(`   curl -s -X POST ${BASE}/api/mcp/oauth/token -d "grant_type=authorization_code&code=EL_CODE&code_verifier=${verifier}&client_id=${reg.client_id}&redirect_uri=https://example.com/cb"`);
  console.log("\n   Y prueba el token:");
  console.log(`   curl -s -X POST ${BASE}/api/mcp -H "Authorization: Bearer EL_ACCESS_TOKEN" -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"get_workspace_overview","arguments":{}}}'`);
}

main().catch((e) => {
  console.error("✗ smoke falló:", e);
  process.exit(1);
});
```

- [ ] **Step 2: Ejecutar el smoke (partes automatizables)**

Run: `pnpm exec tsx scripts/mcp-oauth-smoke.ts`
Expected: `✓ metadata`, `✓ registro client_id`, y las instrucciones para el paso manual.

- [ ] **Step 3: Completar el flujo manual una vez**

Abrir la URL impresa en el navegador (logueado como owner de un workspace), autorizar, copiar el `code`, intercambiarlo por el token y llamar a `get_workspace_overview`.
Expected: el intercambio devuelve `access_token` (`mnt_at_…`) y la llamada MCP devuelve el overview del workspace autorizado.

- [ ] **Step 4: Commit**

```bash
git add scripts/mcp-oauth-smoke.ts
git commit -m "test(mcp): smoke script del flujo OAuth end-to-end"
```

---

## Task 11: Gestión de conexiones OAuth en la UI + abrir a owners

**Files:**
- Modify: `src/actions/mcp.ts`
- Modify: `src/components/workspace/McpKeysPanel.tsx`
- Modify: `src/app/(app)/[workspace]/mcp/page.tsx`
- Modify: `src/components/layout/MainNav.tsx`

**Interfaces:**
- Consumes: tabla `mcp_oauth_tokens` (Task 1).
- Produces: `listMcpConnectionsAction(workspaceId)`, `revokeMcpConnectionAction(id)`; panel de conexiones OAuth; página visible para owners.

- [ ] **Step 1: Cambiar el gate de las actions de super-admin a owners**

En `src/actions/mcp.ts`, reemplazar la función `requireSuperAdmin` y su uso por un gate basado en workspace. Reemplazar `requireSuperAdmin()` por esta función y actualizar las 3 actions existentes (`listMcpKeysAction`, `generateMcpKeyAction`, `revokeMcpKeyAction`) para que reciban/usen `workspaceId`:

```ts
/** Gate: el usuario debe poder gestionar el workspace (owner/admin). */
async function requireManager(workspaceId: string): Promise<{ error: string } | null> {
  const supabase = await createClient();
  const { data: canManage } = await supabase.rpc("can_manage_workspace", {
    p_workspace_id: workspaceId,
  });
  if (!canManage) return { error: "Sin permisos" };
  return null;
}
```

En `listMcpKeysAction` y `generateMcpKeyAction`, cambiar `const gate = await requireSuperAdmin();` por `const gate = await requireManager(workspaceId);`. En `revokeMcpKeyAction`, que recibe `keyId`, primero resolver el workspace de la key vía service client y luego `requireManager`:

```ts
export async function revokeMcpKeyAction(keyId: string): Promise<ActionResult<{ id: string }>> {
  const service = mcpServiceClient();
  const { data: key } = await service.from("mcp_api_keys").select("workspace_id").eq("id", keyId).maybeSingle();
  if (!key) return { success: false, error: "Key no encontrada" };
  const gate = await requireManager(key.workspace_id as string);
  if (gate) return { success: false, error: gate.error };

  const { error } = await service
    .from("mcp_api_keys")
    .update({ revoked_at: new Date().toISOString() })
    .eq("id", keyId);
  if (error) return { success: false, error: error.message };
  return { success: true, data: { id: keyId } };
}
```

Eliminar el import de `isSuperAdmin` si queda sin uso.

- [ ] **Step 2: Añadir las actions de conexiones OAuth**

Añadir al final de `src/actions/mcp.ts`:

```ts
export interface McpConnectionRow {
  id: string;
  clientName: string | null;
  createdAt: string;
  lastUsedAt: string | null;
  revokedAt: string | null;
}

export async function listMcpConnectionsAction(
  workspaceId: string
): Promise<ActionResult<McpConnectionRow[]>> {
  const gate = await requireManager(workspaceId);
  if (gate) return { success: false, error: gate.error };

  const service = mcpServiceClient();
  const { data, error } = await service
    .from("mcp_oauth_tokens")
    .select("id, client_name, created_at, last_used_at, revoked_at")
    .eq("workspace_id", workspaceId)
    .order("created_at", { ascending: false });
  if (error) return { success: false, error: error.message };

  return {
    success: true,
    data: (data ?? []).map((r) => ({
      id: r.id as string,
      clientName: (r.client_name as string | null) ?? null,
      createdAt: r.created_at as string,
      lastUsedAt: (r.last_used_at as string | null) ?? null,
      revokedAt: (r.revoked_at as string | null) ?? null,
    })),
  };
}

export async function revokeMcpConnectionAction(id: string): Promise<ActionResult<{ id: string }>> {
  const service = mcpServiceClient();
  const { data: tok } = await service
    .from("mcp_oauth_tokens")
    .select("workspace_id")
    .eq("id", id)
    .maybeSingle();
  if (!tok) return { success: false, error: "Conexión no encontrada" };
  const gate = await requireManager(tok.workspace_id as string);
  if (gate) return { success: false, error: gate.error };

  const { error } = await service
    .from("mcp_oauth_tokens")
    .update({ revoked_at: new Date().toISOString() })
    .eq("id", id);
  if (error) return { success: false, error: error.message };
  return { success: true, data: { id } };
}
```

- [ ] **Step 3: Cambiar el gate de la página a owners y pasar conexiones**

En `src/app/(app)/[workspace]/mcp/page.tsx`, reemplazar el import y el gate. Quitar `import { isSuperAdmin } from "@/lib/auth/superAdmin";`. Cambiar el bloque del gate por:

```ts
  const { data: workspace } = await supabase
    .from("workspaces")
    .select("id, slug, name")
    .eq("slug", slug)
    .single();

  if (!workspace) notFound();

  const { data: canManage } = await supabase.rpc("can_manage_workspace", {
    p_workspace_id: workspace.id,
  });
  if (!canManage) notFound();
```

Y quitar la obtención de `user` si queda sin uso. Añadir la carga de conexiones junto a `keysResult`:

```ts
  const [keysResult, connectionsResult] = await Promise.all([
    listMcpKeysAction(workspace.id),
    listMcpConnectionsAction(workspace.id),
  ]);
```

Pasar `initialConnections={connectionsResult.data ?? []}` al `<McpKeysPanel>` y actualizar el subtítulo (quitar "En desarrollo — visible solo para administradores").

- [ ] **Step 4: Añadir el panel de conexiones OAuth en McpKeysPanel**

En `src/components/workspace/McpKeysPanel.tsx`, añadir `initialConnections: McpConnectionRow[]` a `Props`, importar `listMcpConnectionsAction, revokeMcpConnectionAction, type McpConnectionRow` de `@/actions/mcp`, y renderizar una `Card` nueva "Conexiones OAuth activas" que mapee las conexiones con su `clientName`, `lastUsedAt` y un botón Revocar (mismo patrón que el listado de keys, usando `revokeMcpConnectionAction`). Reutilizar `formatDate` y el patrón de `handleRevoke` ya presentes en el archivo.

- [ ] **Step 5: Cambiar el ítem de menú a owners**

En `src/components/layout/MainNav.tsx:43`, reemplazar:
```ts
  { href: `/${slug}/mcp`, label: "MCP (beta)", icon: Plug, superAdminOnly: true },
```
por:
```ts
  { href: `/${slug}/mcp`, label: "MCP", icon: Plug, ownerOnly: true },
```

- [ ] **Step 6: Verificar tipos, lint y build**

Run: `pnpm exec tsc --noEmit && pnpm lint && pnpm build`
Expected: sin errores.

- [ ] **Step 7: Verificar acceso por rol en el navegador**

Logueado como owner de un workspace: `/{workspace}/mcp` carga y muestra keys + conexiones OAuth.
Logueado como miembro no-owner: el ítem MCP no aparece y `/{workspace}/mcp` devuelve 404.

- [ ] **Step 8: Commit**

```bash
git add src/actions/mcp.ts src/components/workspace/McpKeysPanel.tsx "src/app/(app)/[workspace]/mcp/page.tsx" src/components/layout/MainNav.tsx
git commit -m "feat(mcp): gestión de conexiones OAuth y apertura de la página MCP a owners"
```

---

## Task 12: Documentación

**Files:**
- Modify: `docs/mcp-server.md`

**Interfaces:** ninguna (solo docs).

- [ ] **Step 1: Añadir la sección OAuth**

En `docs/mcp-server.md`, añadir una sección "Conexión vía OAuth (recomendada para Claude.ai / ChatGPT)" que explique: el cliente solo pega la URL `https://neogeo-three.vercel.app/api/mcp` en Ajustes → Conectores, pulsa Connect, hace login en Mentio y autoriza (solo owners/admins). Mencionar que las API keys `mnt_live_` siguen disponibles para Claude Code / CLI. Actualizar la nota de visibilidad: la página MCP ya no es solo super-admin, es para owners del workspace.

- [ ] **Step 2: Verificar lint (docs no rompe, pero confirmar build limpio)**

Run: `pnpm lint`
Expected: sin errores (los `.md` no los cubre Biome, pero confirma que no hay regresión).

- [ ] **Step 3: Commit**

```bash
git add docs/mcp-server.md
git commit -m "docs(mcp): documentar la conexión OAuth para clientes"
```

---

## Self-Review (completado por el autor del plan)

- **Cobertura de la spec:** flujo del usuario (Tasks 4-9), 5 endpoints (Tasks 4-7), 3 cambios en existentes (Tasks 3, 8, 9), modelo de datos (Task 1), consentimiento owners-only (Task 6), seguridad/PKCE/rotación (Tasks 2, 6, 7), gestión/revocación en UI (Task 11), no-regresión de keys (Task 3 Step 4), docs (Task 12). ✓
- **Placeholders:** ninguno; todo el código está completo salvo Task 11 Step 4 (panel UI) que describe el patrón exacto a replicar de código ya presente en el mismo archivo — es una réplica de un bloque existente, no un placeholder.
- **Consistencia de tipos:** prefijos (`mnt_at_`/`mnt_rt_`/`mnt_ac_`) y helpers (`generateOpaqueToken`, `verifyPkceS256`, `oauthBaseUrl`, `loadWorkspace`) usados de forma consistente entre tareas. Nombres de columnas alineados con la migración de Task 1.
```
